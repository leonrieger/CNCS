#include "settings.hpp"
#include "../OS.hpp" // for operating system specific configs

bool CNCS::settings::interpreter::load_settings(
    user_environment_settings& settings) {
    
}

bool CNCS::settings::interpreter::load_settings(
    global_environment_settings& settings) {}
